<!DOCTYPE html>

<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<title>Minha Rotina Skin Care</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
:root {
--cream: #FAF6F1;
--blush: #E8C5B0;
--rose: #C4806A;
--deep: #3D2B1F;
--muted: #8A6F5E;
--sage: #A8B5A2;
--morning: #FFF3E8;
--night: #1E1420;
--night-card: #2C1F35;
--night-accent: #9B7FBA;
--gold: #D4A96A;
}

- { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

body {
font-family: ‘DM Sans’, sans-serif;
background: var(–cream);
color: var(–deep);
min-height: 100vh;
overflow-x: hidden;
}

/* HEADER */
.header {
background: linear-gradient(135deg, var(–deep) 0%, #6B3D2E 100%);
padding: 36px 20px 28px;
text-align: center;
position: relative;
overflow: hidden;
}
.header::before {
content: ‘’;
position: absolute;
top: -40px; right: -40px;
width: 180px; height: 180px;
border-radius: 50%;
background: rgba(196,128,106,0.2);
}
.header::after {
content: ‘’;
position: absolute;
bottom: -30px; left: -20px;
width: 120px; height: 120px;
border-radius: 50%;
background: rgba(168,181,162,0.15);
}
.header h1 {
font-family: ‘Cormorant Garamond’, serif;
font-size: 2.4rem;
font-weight: 300;
color: var(–cream);
letter-spacing: 0.05em;
position: relative; z-index: 1;
}
.header h1 span { font-style: italic; color: var(–blush); }
.header p {
font-size: 0.9rem;
color: rgba(250,246,241,0.6);
margin-top: 6px;
letter-spacing: 0.15em;
text-transform: uppercase;
position: relative; z-index: 1;
}

/* TABS */
.tabs {
display: flex;
background: var(–deep);
padding: 0 20px 16px;
gap: 10px;
}
.tab {
flex: 1;
padding: 10px;
border: none;
border-radius: 50px;
font-family: ‘DM Sans’, sans-serif;
font-size: 0.95rem;
font-weight: 500;
cursor: pointer;
transition: all 0.3s;
letter-spacing: 0.05em;
}
.tab.morning { background: rgba(250,246,241,0.1); color: var(–blush); }
.tab.morning.active { background: var(–gold); color: var(–deep); }
.tab.night { background: rgba(250,246,241,0.1); color: var(–night-accent); }
.tab.night.active { background: var(–night-accent); color: white; }
.tab.products { background: rgba(250,246,241,0.1); color: var(–sage); }
.tab.products.active { background: var(–sage); color: var(–deep); }

/* CONTENT */
.content { padding: 20px; display: none; }
.content.active { display: block; }

/* DAY SELECTOR (Night) */
.day-selector {
display: flex;
gap: 8px;
margin-bottom: 20px;
overflow-x: auto;
padding-bottom: 4px;
scrollbar-width: none;
}
.day-selector::-webkit-scrollbar { display: none; }
.day-btn {
flex-shrink: 0;
padding: 8px 16px;
border: 1.5px solid var(–night-accent);
border-radius: 50px;
background: transparent;
color: var(–night-accent);
font-family: ‘DM Sans’, sans-serif;
font-size: 0.9rem;
cursor: pointer;
transition: all 0.25s;
white-space: nowrap;
}
.day-btn.active { background: var(–night-accent); color: white; }

/* SECTION LABEL */
.section-label {
display: flex;
align-items: center;
gap: 10px;
margin-bottom: 14px;
}
.section-label .line { flex: 1; height: 1px; background: var(–blush); opacity: 0.4; }
.section-label span {
font-family: ‘Cormorant Garamond’, serif;
font-size: 1.2rem;
font-style: italic;
color: var(–muted);
white-space: nowrap;
}

/* ROUTINE CARD */
.routine-card {
background: white;
border-radius: 16px;
overflow: hidden;
box-shadow: 0 2px 20px rgba(61,43,31,0.06);
margin-bottom: 12px;
}
.night-bg .routine-card { background: var(–night-card); }

.step-item {
display: flex;
align-items: flex-start;
padding: 14px 16px;
border-bottom: 1px solid rgba(61,43,31,0.06);
gap: 14px;
transition: background 0.2s;
position: relative;
}
.night-bg .step-item { border-bottom-color: rgba(255,255,255,0.06); }
.step-item:last-child { border-bottom: none; }
.step-item.done { opacity: 0.45; }
.step-item.done .step-name { text-decoration: line-through; }

.step-num {
width: 34px; height: 34px;
border-radius: 50%;
display: flex; align-items: center; justify-content: center;
font-size: 0.85rem;
font-weight: 500;
flex-shrink: 0;
margin-top: 1px;
}
.morning-num { background: var(–morning); color: var(–rose); }
.night-num { background: rgba(155,127,186,0.2); color: var(–night-accent); }

.step-info { flex: 1; min-width: 0; }
.step-name {
font-size: 1.05rem;
font-weight: 500;
color: var(–deep);
line-height: 1.3;
}
.night-bg .step-name { color: #EDE8F5; }
.step-purpose {
font-size: 0.88rem;
color: var(–muted);
margin-top: 2px;
}
.night-bg .step-purpose { color: rgba(237,232,245,0.5); }
.step-wait {
font-size: 0.83rem;
color: var(–rose);
margin-top: 3px;
font-style: italic;
}
.night-bg .step-wait { color: var(–night-accent); }

.step-check {
width: 30px; height: 30px;
border-radius: 50%;
border: 2px solid var(–blush);
background: transparent;
cursor: pointer;
flex-shrink: 0;
display: flex; align-items: center; justify-content: center;
transition: all 0.2s;
margin-top: 2px;
}
.night-bg .step-check { border-color: var(–night-accent); }
.step-check.checked { background: var(–rose); border-color: var(–rose); }
.night-bg .step-check.checked { background: var(–night-accent); border-color: var(–night-accent); }
.step-check svg { display: none; }
.step-check.checked svg { display: block; }

/* ALERT BOX */
.alert-box {
background: linear-gradient(135deg, #FFF0EB, #FDE8E0);
border-left: 3px solid var(–rose);
border-radius: 12px;
padding: 14px 16px;
margin-bottom: 16px;
font-size: 0.92rem;
color: var(–deep);
line-height: 1.6;
}
.alert-box strong { color: var(–rose); display: block; margin-bottom: 4px; font-size: 0.95rem; }
.night-bg .alert-box { background: rgba(155,127,186,0.1); border-left-color: var(–night-accent); }
.night-bg .alert-box strong { color: var(–night-accent); }
.night-bg .alert-box { color: #EDE8F5; }

/* PROGRESS BAR */
.progress-wrap { margin-bottom: 20px; }
.progress-label {
display: flex;
justify-content: space-between;
font-size: 0.88rem;
color: var(–muted);
margin-bottom: 6px;
}
.night-bg .progress-label { color: rgba(237,232,245,0.5); }
.progress-bar {
height: 4px;
background: rgba(61,43,31,0.1);
border-radius: 2px;
overflow: hidden;
}
.night-bg .progress-bar { background: rgba(255,255,255,0.1); }
.progress-fill {
height: 100%;
border-radius: 2px;
transition: width 0.4s ease;
}
.morning-fill { background: linear-gradient(90deg, var(–gold), var(–rose)); }
.night-fill { background: linear-gradient(90deg, var(–night-accent), #C4A0E8); }

.reset-btn {
width: 100%;
padding: 12px;
border: 1.5px solid var(–blush);
border-radius: 12px;
background: transparent;
color: var(–muted);
font-family: ‘DM Sans’, sans-serif;
font-size: 0.95rem;
cursor: pointer;
margin-top: 8px;
transition: all 0.2s;
}
.reset-btn:active { background: var(–blush); color: var(–deep); }
.night-bg .reset-btn { border-color: rgba(155,127,186,0.3); color: rgba(237,232,245,0.4); }

/* NIGHT BACKGROUND */
.night-bg {
background: var(–night);
min-height: calc(100vh - 120px);
margin: -20px;
padding: 20px;
}

/* PRODUCTS TAB */
.product-item {
background: white;
border-radius: 14px;
padding: 14px 16px;
margin-bottom: 10px;
display: flex;
align-items: center;
gap: 14px;
box-shadow: 0 2px 12px rgba(61,43,31,0.05);
}
.product-emoji { font-size: 1.4rem; flex-shrink: 0; }
.product-info { flex: 1; min-width: 0; }
.product-name {
font-size: 1rem;
font-weight: 500;
color: var(–deep);
line-height: 1.3;
}
.product-use {
font-size: 0.85rem;
color: var(–muted);
margin-top: 2px;
}
.product-tags {
display: flex;
gap: 4px;
flex-wrap: wrap;
margin-top: 5px;
}
.tag {
font-size: 0.75rem;
padding: 2px 8px;
border-radius: 50px;
font-weight: 500;
letter-spacing: 0.04em;
text-transform: uppercase;
}
.tag-morning { background: #FFF3E8; color: var(–rose); }
.tag-night { background: rgba(155,127,186,0.15); color: var(–night-accent); }
.tag-both { background: #EEF3ED; color: #5A7A54; }

.product-actions { display: flex; gap: 6px; flex-shrink: 0; }
.action-btn {
width: 32px; height: 32px;
border-radius: 50%;
border: none;
display: flex; align-items: center; justify-content: center;
cursor: pointer;
font-size: 0.9rem;
transition: all 0.2s;
}
.btn-delete { background: #FEF0EE; }
.btn-delete:active { background: #FDDBD6; }
.btn-empty { background: #FFF8F0; }
.btn-empty:active { background: #FEECD6; }

.product-item.empty-product {
opacity: 0.5;
border: 1.5px dashed var(–blush);
background: transparent;
box-shadow: none;
}

/* ADD PRODUCT */
.add-section {
background: white;
border-radius: 16px;
padding: 16px;
margin-top: 16px;
box-shadow: 0 2px 12px rgba(61,43,31,0.05);
}
.add-section h3 {
font-family: ‘Cormorant Garamond’, serif;
font-size: 1.25rem;
color: var(–deep);
margin-bottom: 12px;
font-weight: 400;
}
.input-field {
width: 100%;
padding: 10px 14px;
border: 1.5px solid var(–blush);
border-radius: 10px;
font-family: ‘DM Sans’, sans-serif;
font-size: 1rem;
color: var(–deep);
background: var(–cream);
margin-bottom: 8px;
outline: none;
transition: border-color 0.2s;
}
.input-field:focus { border-color: var(–rose); }
.select-row { display: flex; gap: 8px; }
.select-field {
flex: 1;
padding: 10px 14px;
border: 1.5px solid var(–blush);
border-radius: 10px;
font-family: ‘DM Sans’, sans-serif;
font-size: 0.95rem;
color: var(–deep);
background: var(–cream);
outline: none;
appearance: none;
}
.add-btn {
width: 100%;
padding: 12px;
background: linear-gradient(135deg, var(–deep), #6B3D2E);
color: var(–cream);
border: none;
border-radius: 12px;
font-family: ‘DM Sans’, sans-serif;
font-size: 1rem;
font-weight: 500;
cursor: pointer;
margin-top: 4px;
letter-spacing: 0.05em;
transition: opacity 0.2s;
}
.add-btn:active { opacity: 0.85; }

/* CALENDAR */
.calendar-card {
background: white;
border-radius: 16px;
overflow: hidden;
box-shadow: 0 2px 20px rgba(61,43,31,0.06);
margin-bottom: 20px;
}
.cal-row {
display: grid;
grid-template-columns: 80px 1fr 1fr;
border-bottom: 1px solid rgba(61,43,31,0.06);
min-height: 44px;
}
.cal-row:last-child { border-bottom: none; }
.cal-day {
padding: 12px;
font-size: 0.88rem;
font-weight: 500;
color: var(–muted);
display: flex; align-items: center;
border-right: 1px solid rgba(61,43,31,0.06);
text-transform: uppercase;
letter-spacing: 0.08em;
}
.cal-cell {
padding: 10px 12px;
display: flex; align-items: center;
font-size: 0.85rem;
line-height: 1.4;
color: var(–deep);
border-right: 1px solid rgba(61,43,31,0.06);
}
.cal-cell:last-child { border-right: none; }
.cal-badge {
font-size: 0.78rem;
padding: 3px 8px;
border-radius: 50px;
font-weight: 500;
display: inline-block;
}
.badge-a { background: rgba(155,127,186,0.15); color: var(–night-accent); }
.badge-b { background: rgba(212,169,106,0.15); color: #A07830; }
.badge-c { background: #EEF3ED; color: #5A7A54; }
.badge-sun { background: #FFF3E8; color: var(–rose); }
.cal-header {
background: var(–deep);
display: grid;
grid-template-columns: 80px 1fr 1fr;
}
.cal-header-cell {
padding: 10px 12px;
font-size: 0.8rem;
letter-spacing: 0.12em;
text-transform: uppercase;
color: rgba(250,246,241,0.6);
font-weight: 500;
}

.empty-state {
text-align: center;
padding: 30px 20px;
color: var(–muted);
font-size: 0.85rem;
font-style: italic;
}

/* TOAST */
.toast {
position: fixed;
bottom: 30px;
left: 50%;
transform: translateX(-50%) translateY(20px);
background: var(–deep);
color: var(–cream);
padding: 10px 20px;
border-radius: 50px;
font-size: 0.8rem;
opacity: 0;
transition: all 0.3s;
z-index: 999;
white-space: nowrap;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

.floating-tip {
background: linear-gradient(135deg, var(–rose), #A05540);
color: white;
border-radius: 14px;
padding: 14px 16px;
font-size: 0.78rem;
line-height: 1.6;
margin-bottom: 16px;
}
.floating-tip strong { display: block; margin-bottom: 4px; font-size: 0.82rem; letter-spacing: 0.04em; }
</style>

</head>
<body>

<div class="header">
<h1>Minha Rotina <span>Skin Care</span></h1>
<p>Personalizada para você ✨</p>
</div>

<div class="tabs">
<button class="tab morning active" onclick="switchTab('morning')">☀️ Manhã</button>
<button class="tab night" onclick="switchTab('night')">🌙 Noite</button>
<button class="tab products" onclick="switchTab('products')">🧴 Produtos</button>
</div>

<!-- MORNING TAB -->

<div class="content active" id="tab-morning">
<div class="progress-wrap">
<div class="progress-label">
<span>Progresso de hoje</span>
<span id="morning-count">0 / 7</span>
</div>
<div class="progress-bar">
<div class="progress-fill morning-fill" id="morning-bar" style="width:0%"></div>
</div>
</div>

<div class="floating-tip">
<strong>☀️ Rotina Diurna — Todos os dias</strong>
Vitamina C + SPF é o combo anti-mancha mais poderoso. Nunca pule o protetor!
</div>

<div class="routine-card" id="morning-list"></div>
<button class="reset-btn" onclick="resetRoutine('morning')">↺ Recomeçar rotina</button>
</div>

<!-- NIGHT TAB -->

<div class="content" id="tab-night">
<div class="night-bg">
<div class="day-selector" id="day-selector">
<button class="day-btn active" onclick="selectDay('A')" id="day-A">Noite A</button>
<button class="day-btn" onclick="selectDay('B')" id="day-B">Noite B</button>
<button class="day-btn" onclick="selectDay('C')" id="day-C">Noite C</button>
<button class="day-btn" onclick="selectDay('cal')" id="day-cal">📅 Calendário</button>
</div>

```
<div id="night-A-content">
<div class="alert-box">
<strong>🌙 Noite A — Retinal + Clareamento</strong>
Seg · Qua · Sex — Foco: linhas de expressão e manchas
</div>
<div class="progress-wrap">
<div class="progress-label">
<span>Progresso</span>
<span id="nightA-count">0 / 9</span>
</div>
<div class="progress-bar">
<div class="progress-fill night-fill" id="nightA-bar" style="width:0%"></div>
</div>
</div>
<div class="routine-card" id="nightA-list"></div>
<button class="reset-btn" onclick="resetRoutine('nightA')">↺ Recomeçar rotina</button>
</div>

<div id="night-B-content" style="display:none">
<div class="alert-box">
<strong>✨ Noite B — BHA + Differin</strong>
Ter · Qui — Foco: poros pretos e renovação celular
</div>
<div class="progress-wrap">
<div class="progress-label">
<span>Progresso</span>
<span id="nightB-count">0 / 8</span>
</div>
<div class="progress-bar">
<div class="progress-fill night-fill" id="nightB-bar" style="width:0%"></div>
</div>
</div>
<div class="routine-card" id="nightB-list"></div>
<button class="reset-btn" onclick="resetRoutine('nightB')">↺ Recomeçar rotina</button>
</div>

<div id="night-C-content" style="display:none">
<div class="alert-box">
<strong>💤 Noite C — Descanso</strong>
Sáb · Dom — Só hidratação, sem ativos
</div>
<div class="progress-wrap">
<div class="progress-label">
<span>Progresso</span>
<span id="nightC-count">0 / 5</span>
</div>
<div class="progress-bar">
<div class="progress-fill night-fill" id="nightC-bar" style="width:0%"></div>
</div>
</div>
<div class="routine-card" id="nightC-list"></div>
<button class="reset-btn" onclick="resetRoutine('nightC')">↺ Recomeçar rotina</button>
</div>

<div id="night-cal-content" style="display:none">
<div class="section-label">
<div class="line"></div>
<span>Calendário Semanal</span>
<div class="line"></div>
</div>
<div class="calendar-card">
<div class="cal-header">
<div class="cal-header-cell">Dia</div>
<div class="cal-header-cell">☀️ Manhã</div>
<div class="cal-header-cell">🌙 Noite</div>
</div>
<div class="cal-row"><div class="cal-day">Seg</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-a">Noite A — Retinal</span></div></div>
<div class="cal-row"><div class="cal-day">Ter</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-b">Noite B — BHA</span></div></div>
<div class="cal-row"><div class="cal-day">Qua</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-a">Noite A — Retinal</span></div></div>
<div class="cal-row"><div class="cal-day">Qui</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-b">Noite B — BHA</span></div></div>
<div class="cal-row"><div class="cal-day">Sex</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-a">Noite A — Retinal</span></div></div>
<div class="cal-row"><div class="cal-day">Sáb</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-c">Noite C — Descanso</span></div></div>
<div class="cal-row"><div class="cal-day">Dom</div><div class="cal-cell"><span class="cal-badge badge-sun">Rotina Diurna</span></div><div class="cal-cell"><span class="cal-badge badge-c">Noite C — Descanso</span></div></div>
</div>
<div class="alert-box">
<strong>⚠️ Regras de ouro</strong>
Nunca use Retinal e Differin na mesma noite · SPF todo dia sem exceção · Retinol Shot guarde para alternar com o Retinal quando acabar o Differin
</div>
</div>
```

</div>
</div>

<!-- PRODUCTS TAB -->

<div class="content" id="tab-products">
<div class="section-label">
<div class="line"></div>
<span>Seus produtos</span>
<div class="line"></div>
</div>
<div id="products-list"></div>

<div class="add-section">
<h3>+ Adicionar produto</h3>
<input class="input-field" id="new-product-name" placeholder="Nome do produto" />
<input class="input-field" id="new-product-use" placeholder="Para que serve (ex: hidratante)" />
<div class="select-row">
<select class="select-field" id="new-product-when">
<option value="morning">☀️ Manhã</option>
<option value="night">🌙 Noite</option>
<option value="both">☀️🌙 Ambos</option>
</select>
<select class="select-field" id="new-product-emoji">
<option value="🧴">🧴</option>
<option value="✨">✨</option>
<option value="💧">💧</option>
<option value="🌙">🌙</option>
<option value="☀️">☀️</option>
<option value="🔬">🔬</option>
<option value="💊">💊</option>
<option value="🫧">🫧</option>
</select>
</div>
<button class="add-btn" onclick="addProduct()">Adicionar produto</button>
</div>
</div>

<div class="toast" id="toast"></div>

<!-- MODAL -->

<div id="modal-overlay" style="display:none; position:fixed; inset:0; background:rgba(0,0,0,0.5); z-index:1000; align-items:flex-end; justify-content:center;">
<div id="modal-box" style="background:white; border-radius:24px 24px 0 0; padding:28px 24px 40px; width:100%; max-width:480px; box-shadow:0 -8px 40px rgba(0,0,0,0.2);">
<p id="modal-title" style="font-family:'Cormorant Garamond',serif; font-size:1.3rem; color:#3D2B1F; margin-bottom:6px; font-weight:400;"></p>
<p id="modal-sub" style="font-size:0.9rem; color:#8A6F5E; margin-bottom:22px; line-height:1.5;"></p>
<div id="modal-buttons" style="display:flex; flex-direction:column; gap:10px;"></div>
</div>
</div>

<script>
// ---- STATE ----
const DEFAULT_STATE = {
morningChecked: {}, nightAChecked: {}, nightBChecked: {}, nightCChecked: {},
skippedSteps: [], // keys of steps removed due to out-of-stock
products: [
{ id: 1, name: 'Neutrogena Oil-Free Eye Makeup Remover', use: 'Demaquilante olhos', when: 'both', emoji: '💧', status: 'ok' },
{ id: 2, name: 'CeraVe Balancing Air Foam Cleanser', use: 'Limpeza facial', when: 'both', emoji: '🫧', status: 'ok' },
{ id: 3, name: 'Thayers Facial Toner', use: 'Equilibrar pH da pele', when: 'both', emoji: '🧴', status: 'ok' },
{ id: 4, name: 'Vichy Vitamina C', use: 'Manchas + luminosidade', when: 'both', emoji: '✨', status: 'ok' },
{ id: 5, name: 'Celimax Pore Dark Spot', use: 'Poros + manchas', when: 'night', emoji: '🔬', status: 'ok' },
{ id: 6, name: 'The Ordinary Factors + Beta Glucan', use: 'Hidratação + peptídeos', when: 'both', emoji: '💧', status: 'ok' },
{ id: 7, name: 'Celimax Retinal', use: 'Linhas de expressão + manchas', when: 'night', emoji: '🌙', status: 'ok' },
{ id: 8, name: 'CeraVe Eye Repair Cream', use: 'Olheiras', when: 'both', emoji: '👁️', status: 'ok' },
{ id: 9, name: 'La Roche-Posay Lipikar', use: 'Barreira + hidratação final', when: 'night', emoji: '🧴', status: 'ok' },
{ id: 10, name: 'COSRX BHA Blackhead Power Liquid', use: 'Poros pretos', when: 'night', emoji: '🔬', status: 'ok' },
{ id: 11, name: 'The Ordinary Niacinamide 10% + Zinc 1%', use: 'Poros + oleosidade', when: 'both', emoji: '✨', status: 'ok' },
{ id: 12, name: 'Differin Gel (Adapaleno)', use: 'Linhas de expressão', when: 'night', emoji: '🌙', status: 'ok' },
{ id: 13, name: 'La Roche-Posay Cicaplast', use: 'Acalmar + reparar barreira', when: 'night', emoji: '🧴', status: 'ok' },
{ id: 14, name: 'CeraVe Ultra-Light Moisturizing', use: 'Hidratação leve', when: 'morning', emoji: '💧', status: 'ok' },
{ id: 15, name: 'La Roche-Posay SPF 50 Tinted Mineral', use: 'Proteção solar', when: 'morning', emoji: '☀️', status: 'ok' },
{ id: 16, name: 'CeraVe Facial Lotion SPF 30', use: 'Proteção solar (corpo)', when: 'morning', emoji: '☀️', status: 'ok' },
{ id: 17, name: 'Celimax Retinol Shot Serum', use: 'Reserva (alternar com Retinal)', when: 'night', emoji: '🌙', status: 'ok' },
{ id: 18, name: 'Abib Airy Sunstick SPF50+', use: 'Proteção solar — leve e seco', when: 'morning', emoji: '☀️', status: 'ok' },
{ id: 19, name: 'Abib Quick Sunstick SPF50+', use: 'Proteção solar — reaplique fácil', when: 'morning', emoji: '☀️', status: 'ok' },
]
};

const STATE_VER = 5;
let state = (() => {
try {
const saved = localStorage.getItem('skincare-v2');
if (saved) {
const parsed = JSON.parse(saved);
if (parsed._v === STATE_VER) return parsed;
}
} catch(e) {}
const fresh = JSON.parse(JSON.stringify(DEFAULT_STATE));
fresh._v = STATE_VER;
return fresh;
})();

state.products.forEach(p => { if (!p.status) p.status = p.empty ? 'empty' : 'ok'; delete p.empty; });
if (!state.skippedSteps) state.skippedSteps = [];
state._v = STATE_VER;

// ---- BASE STEPS (full list) ----
const BASE_MORNING = [
{ key: 'm1', name: 'CeraVe Air Foam Cleanser', purpose: 'Limpeza leve matinal' },
{ key: 'm2', name: 'Thayers Facial Toner', purpose: 'Equilibrar pH' },
{ key: 'm3', name: 'Vichy Vitamina C', purpose: 'Manchas + proteção antioxidante' },
{ key: 'm4', name: 'The Ordinary Niacinamide 10% + Zinc', purpose: 'Poros + uniformizar' },
{ key: 'm5', name: 'The Ordinary Factors + Beta Glucan', purpose: 'Hidratação + peptídeos' },
{ key: 'm6', name: 'CeraVe Eye Repair Cream', purpose: 'Olheiras' },
{ key: 'm7', name: 'Protetor Solar SPF 50+', purpose: '☀️ La Roche SPF 50 · Abib Airy · Abib Quick — nunca pule!' },
];
const BASE_NIGHT_A = [
{ key: 'na1', name: 'Neutrogena Eye Makeup Remover', purpose: 'Demaquilar olhos' },
{ key: 'na2', name: 'CeraVe Air Foam Cleanser', purpose: 'Limpeza' },
{ key: 'na3', name: 'Thayers Facial Toner', purpose: 'Equilibrar pH' },
{ key: 'na4', name: 'Vichy Vitamina C', purpose: 'Manchas + luminosidade' },
{ key: 'na5', name: 'Celimax Pore Dark Spot', purpose: 'Poros + manchas' },
{ key: 'na6', name: 'The Ordinary Factors + Beta Glucan', purpose: 'Hidratação + peptídeos' },
{ key: 'na7', name: 'Celimax Retinal', purpose: 'Linhas de expressão + manchas', wait: '⏳ Aplique na pele completamente seca' },
{ key: 'na8', name: 'CeraVe Eye Repair Cream', purpose: 'Olheiras' },
{ key: 'na9', name: 'La Roche-Posay Lipikar', purpose: 'Hidratação final + barreira' },
];
const BASE_NIGHT_B = [
{ key: 'nb1', name: 'Neutrogena Eye Makeup Remover', purpose: 'Demaquilar olhos' },
{ key: 'nb2', name: 'CeraVe Air Foam Cleanser', purpose: 'Limpeza' },
{ key: 'nb3', name: 'Thayers Facial Toner', purpose: 'Equilibrar pH' },
{ key: 'nb4', name: 'COSRX BHA Blackhead Power Liquid', purpose: 'Poros pretos', wait: '⏳ Aguarde 20 minutos' },
{ key: 'nb5', name: 'The Ordinary Niacinamide 10% + Zinc', purpose: 'Poros + oleosidade' },
{ key: 'nb6', name: 'Differin Gel', purpose: 'Linhas de expressão (apenas testa)' },
{ key: 'nb7', name: 'CeraVe Eye Repair Cream', purpose: 'Olheiras' },
{ key: 'nb8', name: 'La Roche-Posay Cicaplast', purpose: 'Acalmar + reparar' },
];
const BASE_NIGHT_C = [
{ key: 'nc1', name: 'Neutrogena Eye Makeup Remover', purpose: 'Demaquilar olhos' },
{ key: 'nc2', name: 'CeraVe Air Foam Cleanser', purpose: 'Limpeza' },
{ key: 'nc3', name: 'Thayers Facial Toner', purpose: 'Equilibrar pH' },
{ key: 'nc4', name: 'CeraVe Eye Repair Cream', purpose: 'Olheiras' },
{ key: 'nc5', name: 'La Roche-Posay Lipikar ou Cicaplast', purpose: 'Hidratação noturna' },
];

// Get active steps (filter out skipped)
function getSteps(base) {
return base.filter(s => !state.skippedSteps.includes(s.key));
}
let morningSteps = () => getSteps(BASE_MORNING);
let nightASteps = () => getSteps(BASE_NIGHT_A);
let nightBSteps = () => getSteps(BASE_NIGHT_B);
let nightCSteps = () => getSteps(BASE_NIGHT_C);

function saveState() {
localStorage.setItem('skincare-v2', JSON.stringify(state));
}

// ---- TOAST ----
function showToast(msg) {
const t = document.getElementById('toast');
t.textContent = msg;
t.classList.add('show');
setTimeout(() => t.classList.remove('show'), 2500);
}

// ---- MODAL ----
function showModal(title, sub, buttons) {
document.getElementById('modal-title').textContent = title;
document.getElementById('modal-sub').textContent = sub;
const btnContainer = document.getElementById('modal-buttons');
btnContainer.innerHTML = '';
buttons.forEach(b => {
const btn = document.createElement('button');
btn.textContent = b.label;
btn.style.cssText = `width:100%; padding:14px; border-radius:12px; border:none; font-family:'DM Sans',sans-serif; font-size:1rem; font-weight:500; cursor:pointer; transition:opacity 0.2s; ${b.style || 'background:#FAF6F1; color:#3D2B1F;'}`;
btn.onclick = () => { closeModal(); b.action(); };
btnContainer.appendChild(btn);
});
const overlay = document.getElementById('modal-overlay');
overlay.style.display = 'flex';
}
function closeModal() {
document.getElementById('modal-overlay').style.display = 'none';
}
document.getElementById('modal-overlay').addEventListener('click', function(e) {
if (e.target === this) closeModal();
});

// ---- RENDER ROUTINES ----
function renderSteps(stepsArr, checkedObj, listId, countId, barId, numClass) {
const list = document.getElementById(listId);
if (!list) return;
list.innerHTML = '';
if (stepsArr.length === 0) {
list.innerHTML = '<div style="padding:20px; text-align:center; color:#8A6F5E; font-style:italic; font-size:0.95rem;">Nenhum passo ativo</div>';
document.getElementById(countId).textContent = '0 / 0';
document.getElementById(barId).style.width = '0%';
return;
}
stepsArr.forEach((step, i) => {
const checked = checkedObj[step.key] || false;
const div = document.createElement('div');
div.className = 'step-item' + (checked ? ' done' : '');
div.innerHTML = `
<div class="step-num ${numClass}">${i + 1}</div>
<div class="step-info">
<div class="step-name">${step.name}</div>
<div class="step-purpose">${step.purpose}</div>
${step.wait ? `<div class="step-wait">${step.wait}</div>` : ''}
</div>
<button class="step-check ${checked ? 'checked' : ''}" onclick="toggleStep('${step.key}', '${listId}', '${countId}', '${barId}', '${numClass}')">
<svg width="12" height="9" viewBox="0 0 12 9" fill="none">
<path d="M1 4L4.5 7.5L11 1" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
</button>
`;
list.appendChild(div);
});
updateProgress(checkedObj, stepsArr, countId, barId);
}

function toggleStep(key, listId, countId, barId, numClass) {
const routineMap = {
'morning-list': ['morningChecked', morningSteps()],
'nightA-list': ['nightAChecked', nightASteps()],
'nightB-list': ['nightBChecked', nightBSteps()],
'nightC-list': ['nightCChecked', nightCSteps()],
};
const [stateKey, steps] = routineMap[listId];
state[stateKey][key] = !state[stateKey][key];
saveState();
renderSteps(steps, state[stateKey], listId, countId, barId, numClass);
if (state[stateKey][key]) showToast('✓ Passo concluído!');
}

function updateProgress(checkedObj, steps, countId, barId) {
const done = steps.filter(s => checkedObj[s.key]).length;
const total = steps.length;
document.getElementById(countId).textContent = `${done} / ${total}`;
document.getElementById(barId).style.width = total ? `${(done/total)*100}%` : '0%';
}

function resetRoutine(which) {
const map = {
morning: ['morningChecked', morningSteps(), 'morning-list', 'morning-count', 'morning-bar', 'morning-num'],
nightA: ['nightAChecked', nightASteps(), 'nightA-list', 'nightA-count', 'nightA-bar', 'night-num'],
nightB: ['nightBChecked', nightBSteps(), 'nightB-list', 'nightB-count', 'nightB-bar', 'night-num'],
nightC: ['nightCChecked', nightCSteps(), 'nightC-list', 'nightC-count', 'nightC-bar', 'night-num'],
};
const [sk, steps, lid, cid, bid, nc] = map[which];
state[sk] = {};
saveState();
renderSteps(steps, state[sk], lid, cid, bid, nc);
showToast('↺ Rotina reiniciada');
}

function renderAllRoutines() {
renderSteps(morningSteps(), state.morningChecked, 'morning-list', 'morning-count', 'morning-bar', 'morning-num');
renderSteps(nightASteps(), state.nightAChecked, 'nightA-list', 'nightA-count', 'nightA-bar', 'night-num');
renderSteps(nightBSteps(), state.nightBChecked, 'nightB-list', 'nightB-count', 'nightB-bar', 'night-num');
renderSteps(nightCSteps(), state.nightCChecked, 'nightC-list', 'nightC-count', 'nightC-bar', 'night-num');
}

// ---- SYNC ROUTINE WITH STOCK ----
// Maps product name fragments to step keys they affect
const PRODUCT_STEP_MAP = [
{ fragment: 'Neutrogena', keys: ['na1','nb1','nc1'] },
{ fragment: 'Air Foam', keys: ['m1','na2','nb2','nc2'] },
{ fragment: 'Thayers', keys: ['m2','na3','nb3','nc3'] },
{ fragment: 'Vitamina C', keys: ['m3','na4'] },
{ fragment: 'Pore Dark Spot', keys: ['na5'] },
{ fragment: 'Beta Glucan', keys: ['m5','na6'] },
{ fragment: 'Retinal', keys: ['na7'] },
{ fragment: 'Eye Repair', keys: ['m6','na8','nb7','nc4'] },
{ fragment: 'Lipikar', keys: ['na9','nc5'] },
{ fragment: 'BHA', keys: ['nb4'] },
{ fragment: 'Niacinamide', keys: ['m4','nb5'] },
{ fragment: 'Differin', keys: ['nb6'] },
{ fragment: 'Cicaplast', keys: ['nb8'] },
{ fragment: 'Ultra-Light', keys: ['m5'] },
{ fragment: 'SPF 50', keys: ['m7'] },
{ fragment: 'Airy Sunstick', keys: ['m7'] },
{ fragment: 'Quick Sunstick', keys: ['m7'] },
];

function syncRoutineWithStock() {
// Find all unavailable products (empty or deleted)
const unavailableNames = state.products
.filter(p => p.status === 'empty' || p.status === 'gone')
.map(p => p.name.toLowerCase());

// Group mappings by step key — skip a step only if ALL products for that step are unavailable
const stepProducts = {}; // key -> { total, unavailable }
PRODUCT_STEP_MAP.forEach(mapping => {
const frag = mapping.fragment.toLowerCase();
// Check if ANY product in stock matches this fragment
const allForFrag = state.products.filter(p => p.name.toLowerCase().includes(frag));
const unavailForFrag = allForFrag.filter(p => p.status === 'empty' || p.status === 'gone');
mapping.keys.forEach(k => {
if (!stepProducts[k]) stepProducts[k] = { total: 0, unavailable: 0 };
stepProducts[k].total += allForFrag.length;
stepProducts[k].unavailable += unavailForFrag.length;
});
});

const newSkipped = [];
Object.entries(stepProducts).forEach(([key, counts]) => {
// Skip step only if all mapped products are unavailable
if (counts.total > 0 && counts.unavailable >= counts.total) newSkipped.push(key);
});

state.skippedSteps = [...new Set(newSkipped)];
saveState();
renderAllRoutines();

const count = state.skippedSteps.length;
showToast(count > 0 ? `🔄 Rotina atualizada — ${count} passo(s) ajustado(s)` : '✅ Rotina sincronizada com estoque!');
}

// ---- PRODUCTS ----
function statusLabel(status) {
if (status === 'empty') return '<span style="font-size:0.75rem;color:#C4806A;font-weight:500"> · ACABOU</span>';
if (status === 'gone') return '<span style="font-size:0.75rem;color:#999;font-weight:500"> · NÃO VOU REPOR</span>';
return '';
}
function statusStyle(status) {
if (status === 'empty') return ' empty-product';
if (status === 'gone') return ' empty-product';
return '';
}

function renderProducts() {
const list = document.getElementById('products-list');
list.innerHTML = '';
if (state.products.length === 0) {
list.innerHTML = '<div class="empty-state">Nenhum produto cadastrado ainda.</div>';
return;
}

// Show sync button if any products are unavailable
const hasUnavailable = state.products.some(p => p.status !== 'ok');
if (hasUnavailable) {
const syncDiv = document.createElement('div');
syncDiv.innerHTML = `
<button onclick="syncRoutineWithStock()" style="width:100%;padding:14px;background:linear-gradient(135deg,#3D2B1F,#6B3D2E);color:#FAF6F1;border:none;border-radius:14px;font-family:'DM Sans',sans-serif;font-size:1rem;font-weight:500;cursor:pointer;margin-bottom:16px;letter-spacing:0.04em;">
🔄 Atualizar rotina com meu estoque
</button>
`;
list.appendChild(syncDiv);
}

state.products.forEach(p => {
const div = document.createElement('div');
div.className = 'product-item' + statusStyle(p.status);
const whenLabel = p.when === 'morning' ? '<span class="tag tag-morning">Manhã</span>' :
p.when === 'night' ? '<span class="tag tag-night">Noite</span>' :
'<span class="tag tag-both">Manhã + Noite</span>';

const emptyIcon = p.status === 'ok' ? '🪫' : p.status === 'empty' ? '✅' : '✅';
const emptyTitle = p.status === 'ok' ? 'Marcar como acabou' : 'Marcar como disponível';

div.innerHTML = `
<div class="product-emoji">${p.emoji}</div>
<div class="product-info">
<div class="product-name">${p.name}${statusLabel(p.status)}</div>
<div class="product-use">${p.use}</div>
<div class="product-tags">${whenLabel}</div>
</div>
<div class="product-actions">
<button class="action-btn btn-empty" onclick="changeStatus(${p.id})" title="${emptyTitle}" style="font-size:1.1rem;">
${p.status === 'ok' ? '🪫' : '✅'}
</button>
<button class="action-btn btn-delete" onclick="askDelete(${p.id})" title="Excluir" style="font-size:1.1rem;">🗑️</button>
</div>
`;
list.appendChild(div);
});
}

function changeStatus(id) {
const p = state.products.find(x => x.id === id);
if (!p) return;

if (p.status === 'ok') {
// Ask: acabou or não vai repor?
showModal(
'O que aconteceu com este produto?',
p.name,
[
{
label: '🪫 Acabou — vou comprar de novo',
style: 'background:#FFF3E8; color:#C4806A;',
action: () => { p.status = 'empty'; saveState(); renderProducts(); showToast('🪫 Marcado como acabou'); }
},
{
label: '🚫 Não vou repor este produto',
style: 'background:#F5F0FA; color:#7A5FA0;',
action: () => { p.status = 'gone'; saveState(); renderProducts(); showToast('🚫 Produto marcado como não reposto'); }
},
{
label: 'Cancelar',
style: 'background:#FAF6F1; color:#8A6F5E;',
action: () => {}
}
]
);
} else {
// Restore
p.status = 'ok';
// Remove from skipped
const allKeys = PRODUCT_STEP_MAP.filter(m => p.name.toLowerCase().includes(m.fragment.toLowerCase())).flatMap(m => m.keys);
state.skippedSteps = state.skippedSteps.filter(k => !allKeys.includes(k));
saveState();
renderProducts();
renderAllRoutines();
showToast('✅ Produto disponível novamente!');
}
}

function askDelete(id) {
const p = state.products.find(x => x.id === id);
if (!p) return;
showModal(
'Excluir produto?',
p.name + ' será removido permanentemente da lista.',
[
{
label: '🗑️ Sim, excluir',
style: 'background:#FDDBD6; color:#8B2A1A;',
action: () => {
state.products = state.products.filter(x => x.id !== id);
saveState();
renderProducts();
showToast('🗑️ Produto excluído');
}
},
{
label: 'Cancelar',
style: 'background:#FAF6F1; color:#8A6F5E;',
action: () => {}
}
]
);
}

function addProduct() {
const name = document.getElementById('new-product-name').value.trim();
const use = document.getElementById('new-product-use').value.trim();
const when = document.getElementById('new-product-when').value;
const emoji= document.getElementById('new-product-emoji').value;
if (!name) { showToast('⚠️ Digite o nome do produto'); return; }
state.products.push({ id: Date.now(), name, use: use || '', when, emoji, status: 'ok' });
saveState();
renderProducts();
document.getElementById('new-product-name').value = '';
document.getElementById('new-product-use').value = '';
showToast('✨ Produto adicionado!');
}

// ---- TABS & DAY ----
function switchTab(tab) {
document.querySelectorAll('.content').forEach(c => c.classList.remove('active'));
document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
document.getElementById('tab-' + tab).classList.add('active');
document.querySelector('.tab.' + tab).classList.add('active');
}

let currentDay = 'A';
function selectDay(day) {
currentDay = day;
document.querySelectorAll('.day-btn').forEach(b => b.classList.remove('active'));
document.getElementById('day-' + day).classList.add('active');
['A','B','C','cal'].forEach(d => {
const el = document.getElementById('night-' + d + '-content');
if (el) el.style.display = d === day ? 'block' : 'none';
});
}

// ---- INIT ----
renderAllRoutines();
renderProducts();
</script>

</body>
</html>
